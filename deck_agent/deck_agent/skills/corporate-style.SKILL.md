# corporate-style skill (PLACEHOLDER)

> Replace with your real brand guidelines.

Governs ALL visual and formatting choices. The style tokens below are the ONLY
ones the renderer supports — never invent a token not listed here. Each token
family applies only to its element type: a chart token on a table (or any
token on a `title`, which takes no style) is a validation error.

## Chart style tokens (chart elements only)
- `corporate_ts`        — time-series line charts
- `corporate_bar`       — categorical bar charts
- `corporate_waterfall` — waterfall / bridge charts

## Table style tokens (table elements only)
- `risk_standard` — default risk-report table
- `risk_compact`  — tighter style for dense tables

## Text style tokens (text and kpi elements)
- `body`, `callout`, `footnote`

## Palette / fonts
TODO: document the approved palette, fonts, logo placement, and spacing rules.
